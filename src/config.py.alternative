# 代替案：アバターのカスタムパラメータに Zap 強度値を送信

# === 現在の方式 ===
# /chatbox/input に送信
# → 入力欄に入力されるだけで自動送信されない ✗

# === 代替案 1: カスタムパラメータで送信 ===
# /avatar/parameters/ZapIntensity (float 0.0～1.0) に送信
# → アバター側でウィジェットとして表示 ✓

# === 代替案 2: 複数パラメータで制御 ===
# /chatbox/input に送信
# /chatbox/typing を True → False に切り替え
# → typing の状態変化で送信トリガー？

# === 推奨 ===
# アバターのカスタムパラメータを使用（アバター制作者が設定）
